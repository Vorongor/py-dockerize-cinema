version: "3.8"

services:
  cinema:
    build:
      context: .
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - ./.env
    command: >
      sh -c "python manage.py wait_for_db &&
             python manage.py migrate &&
             python manage.py runserver 0.0.0.0:8000"
    volumes:
      - .:/app
      - static_data:/vol/web/static
      - media_data:/vol/web/media
    depends_on:
      - db

  db:
    image: postgres:14-alpine
    restart: always
    ports:
      - "5433:5432"
    env_file:
      - .env
    volumes:
      - postgres_db_data:$PGDATA

volumes:
  postgres_db_data:
  static_data:
  media_data:
